# Optional heavy deps — install only when you want real AI features:
#   - text → music generation (config: music.backend = local)   → torch, transformers, sentencepiece
#   - AI-art backgrounds       (config: huggingface.enabled)     → diffusers, torch
# CPU-only is fine (the base pipeline needs none of these):
#   pip install -r requirements-ai.txt
torch>=2.1.0
transformers>=4.38.0
sentencepiece>=0.1.99     # MusicGen's T5 text tokenizer
diffusers>=0.27.0
huggingface_hub>=0.20.0

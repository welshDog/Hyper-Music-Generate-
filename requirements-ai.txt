# Optional: GPU/API AI-art backgrounds (diffusion path).
# Only needed when config.yaml has `huggingface.enabled: true`.
# These are heavy (multi-GB) — the base pipeline is fully CPU-only without them.
diffusers>=0.27.0
transformers>=4.38.0
torch>=2.1.0
huggingface_hub>=0.20.0
